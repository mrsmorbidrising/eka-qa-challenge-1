const globals = require('./settings/globals.js');
yarn remove dep
module.exports = function (tahtiana, brown)
  'Navigates To The EKA Site': (browser) => {
    return browser
      .url(globals.url)
      .waitForElementVisible('.nav-logo')
    .("#navbar_contactus_btn").on("click",function(e){
    "how can we help" ();
     (#join-mailinglist_input) 
        .on ("click",function(e){#join-mailinglist_btn}
     $('Request a demo').modal(options)
      .end()
